[pytest]
markers =
	local: marks test as testing a small step so can be run without the cluster, and with travis
	cluster: marks test as testing a large step so requires running on a cluster, and not with travis
